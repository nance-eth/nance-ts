export interface Proposal {
  hash: string,
  title?: string,
  url?: string,
  category?: string,
  status?: string,
  proposalId?: string
  author?: string,
  discussionThreadURL?: string,
  ipfsURL?: string,
  snapshotURL?: string,
}
